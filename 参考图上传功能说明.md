# 参考图上传功能说明

## 功能概述

为AI图像生成系统添加了参考图上传功能，支持用户上传参考图片来辅助图生图过程。

## 修改内容

### 1. 前端界面修改 (`static/index.html`)

#### 首页输入框区域
- 在输入框下方添加了参考图上传区域
- 支持最多3张参考图上传
- 提供图片预览和删除功能

#### 对话页面
- 在输入框区域下方添加了参考图上传区域
- 与首页功能一致

#### 戳戳秀页面
- 重新调整了布局：
  - 参考图上传区域（可选，最多5张）
  - 上传图片区域（原有功能）
  - 额外描述区域（包含模型选择和配置）
- 将模型选择和配置按钮移到了"额外描述"区域内

### 2. JavaScript功能添加 (`static/js/app.js`)

#### 全局变量
```javascript
let homeRefImages = [];      // 首页参考图
let chatRefImages = [];      // 对话页参考图
let pokeArtRefImages = [];   // 戳戳秀参考图
```

#### 新增函数
- `handleHomeRefImageUpload()` - 处理首页参考图上传
- `updateHomeRefImagesPreview()` - 更新首页参考图预览
- `removeHomeRefImage()` - 删除首页参考图
- `handleChatRefImageUpload()` - 处理对话页参考图上传
- `updateChatRefImagesPreview()` - 更新对话页参考图预览
- `removeChatRefImage()` - 删除对话页参考图
- `handlePokeArtRefUpload()` - 处理戳戳秀参考图上传
- `updatePokeArtRefImagesPreview()` - 更新戳戳秀参考图预览
- `removePokeArtRefImage()` - 删除戳戳秀参考图

#### 修改函数
- `sendChatMessage()` - 支持传递参考图到对话页面
- `sendToAI()` - 支持发送参考图到后端API
- `sendChatPageMessage()` - 支持发送参考图
- `processPokeArt()` - 支持在戳戳秀生成时使用参考图

### 3. CSS样式添加 (`static/css/style.css`)

添加了参考图上传区域的样式：
- `.ref-image-upload-section` - 参考图上传区域容器
- `.ref-upload-btn` - 上传按钮样式
- `.ref-preview-item` - 参考图预览项样式
- 包含悬停效果和过渡动画

### 4. 后端API修改 (`app.py`)

#### `chat_route()` 函数修改
- 添加了参考图接收逻辑
- 支持 `ref_image0`, `ref_image1`, ... 多个参考图文件
- 添加 `ref_image_count` 参数来指定参考图数量
- Leonardo模型处理时，如果没有主图片但有参考图，将使用参考图进行图生图

## 使用方法

### 首页/对话页面
1. 在输入框中输入描述文字
2. （可选）点击"上传参考图"按钮选择1-3张参考图
3. （可选）上传主图片
4. 点击发送，AI将根据参考图和描述生成新图片

### 戳戳秀页面
1. （可选）上传参考图（最多5张）
2. 上传要处理的图片（最多10张）
3. 在"额外描述"区域输入额外要求
4. 在"额外描述"区域选择模型和配置参数
5. 点击"开始生成戳戳秀"

## 技术细节

### 前端数据传输
- 参考图通过 `FormData` 以 `ref_image0`, `ref_image1`, ... 形式传输
- 使用 `ref_image_count` 字段标识参考图数量

### 后端处理
- 参考图与主图片分开接收
- Leonardo模型：参考图作为 `image_base64s` 传递给模型
- 处理完成后自动清空参考图数据

### 文件限制
- 单个图片大小：最大 5MB
- 参考图数量：
  - 首页/对话页：最多 3 张
  - 戳戳秀：最多 5 张
- 支持格式：所有常见图片格式（jpg, png, webp等）

## 注意事项

1. 参考图是可选的，不上传参考图时功能保持原样
2. 参考图会在生成完成后自动清空
3. 参考图主要用于Leonardo模型的图生图功能
4. 戳戳秀页面的布局已优化，模型选择更加直观

## 更新日期
2026-01-13

