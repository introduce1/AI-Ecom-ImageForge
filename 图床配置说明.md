# 📸 图床配置说明

## ❌ 问题：SM.MS 图床现在需要登录

从日志看到：
```
[SM.MS] 返回结果: {
    'success': False, 
    'code': 'unauthorized', 
    'message': 'You must be logged in to use.'
}
```

**SM.MS 已经改为需要认证才能使用。**

---

## ✅ 解决方案

### **方案 1：使用本地存储（推荐，可立即测试）✨**

**优点：** 无需注册、立即可用、完全免费

**已经配置好了！**

`app.py` 第 114 行已设置：
```python
USE_LOCAL_STORAGE = True  # 使用本地存储
```

**直接重启服务器即可使用！**

```bash
# 停止当前服务器（Ctrl+C）
# 重新启动
python app.py
```

---

### **方案 2：使用 ImgBB 图床（推荐长期使用）**

**优点：** 免费、稳定、不占用本地空间

#### **步骤 1：注册获取 API Key**

1. 访问：https://api.imgbb.com/
2. 注册账号
3. 获取你的 **API Key**

#### **步骤 2：配置 API Key**

打开 `image_uploader.py`，第 **20 行**：

```python
'imgbb': '粘贴你的API_KEY到这里',
```

#### **步骤 3：启用 ImgBB**

打开 `app.py`，第 **114 行**：

```python
USE_LOCAL_STORAGE = False  # 改为 False

# 第 121 行改为：
image_uploader = ImageUploader('imgbb')  # 使用 ImgBB
```

#### **步骤 4：重启服务器**

```bash
python app.py
```

---

### **方案 3：使用 Imgur 图床**

**优点：** 知名服务、稳定可靠

#### **步骤 1：注册获取 Client ID**

1. 访问：https://api.imgur.com/oauth2/addclient
2. 注册并创建应用
3. 获取你的 **Client ID**

#### **步骤 2：配置 Client ID**

打开 `image_uploader.py`，第 **23 行**：

```python
'imgur': '粘贴你的Client_ID到这里',
```

#### **步骤 3：启用 Imgur**

打开 `app.py`，第 **114 行**：

```python
USE_LOCAL_STORAGE = False

# 第 121 行改为：
image_uploader = ImageUploader('imgur')  # 使用 Imgur
```

---

## 🚀 快速开始（使用本地存储）

### **1. 停止当前服务器**

在终端按 `Ctrl + C`

### **2. 确认配置**

`app.py` 第 114 行应该是：
```python
USE_LOCAL_STORAGE = True  # ✅ 已配置
```

### **3. 重启服务器**

```bash
cd d:\Agent\webapp
python app.py
```

### **4. 测试功能**

浏览器打开 `http://localhost:5000`

点击「去瑕疵」→ 上传图片 → 一键去瑕疵

**应该能正常工作了！**

---

## 📊 本地存储 vs 在线图床

| 特性 | 本地存储 | ImgBB | Imgur |
|------|---------|-------|-------|
| **配置难度** | ✅ 零配置 | ⚠️ 需注册 | ⚠️ 需注册 |
| **速度** | ⚡ 极快 | 🌐 中等 | 🌐 中等 |
| **免费额度** | ✅ 无限 | ✅ 无限 | ⚠️ 有限制 |
| **存储位置** | 💾 本地 | ☁️ 云端 | ☁️ 云端 |
| **是否占用空间** | ⚠️ 是 | ✅ 否 | ✅ 否 |
| **适用场景** | 测试开发 | 生产部署 | 生产部署 |

---

## 🔍 如何查看上传的图片

### **本地存储**

图片保存在：`d:\Agent\webapp\static\uploads\`

URL 格式：`http://localhost:5000/uploads/文件名.png`

### **在线图床**

图片保存在云端，返回公网 URL

---

## 🐛 故障排查

### **问题：仍然报错 "unauthorized"**

**原因：** 没有重启服务器

**解决：** 
```bash
Ctrl + C  # 停止
python app.py  # 重启
```

### **问题：本地存储后无法访问图片**

**原因：** `static/uploads` 文件夹不存在

**解决：** 代码会自动创建，或手动创建：
```bash
mkdir static\uploads
```

### **问题：想要清理本地上传的图片**

**解决：** 删除 `static/uploads` 文件夹内容
```bash
del /q static\uploads\*.*
```

---

## 💡 推荐配置

### **开发测试阶段**

```python
USE_LOCAL_STORAGE = True  # 使用本地存储
```

**优点：** 快速、简单、无需配置

### **生产部署阶段**

```python
USE_LOCAL_STORAGE = False  # 使用 ImgBB
image_uploader = ImageUploader('imgbb')
```

**优点：** 不占用服务器空间、图片可分享

---

## 🎉 完成！

现在你有 3 个选择：

1. ✅ **立即使用本地存储**（已配置，重启即可）
2. 🌐 **配置 ImgBB**（5分钟，长期推荐）
3. 🌐 **配置 Imgur**（5分钟，备选方案）

**推荐：先用本地存储测试功能，满意后再配置在线图床！**

---

**当前状态：** ✅ 已修复，使用本地存储，重启服务器即可使用！

